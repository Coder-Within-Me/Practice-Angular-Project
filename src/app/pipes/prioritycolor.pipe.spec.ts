import { PrioritycolorPipe } from './prioritycolor.pipe';

describe('PrioritycolorPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioritycolorPipe();
    expect(pipe).toBeTruthy();
  });
});
